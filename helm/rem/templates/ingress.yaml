---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: frontend
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  tls:
    - hosts:
      - {{ .Values.ingressHost }}
      secretName: ingress-tls
  rules:
  - host: {{ .Values.ingressHost }}
    http:
      paths:
      - path: /db/
        backend:
          serviceName: restheart
          servicePort: 8080
      - path: /
        backend:
          serviceName: webapp
          servicePort: 80

---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: search
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/rewrite-target: "/$1/_search break; #"
spec:
  tls:
    - hosts:
      - {{ .Values.ingressHost }}
      secretName: ingress-tls
  rules:
  - host: {{ .Values.ingressHost }}
    http:
      paths:
      - path: /es/([a-z]+)/_search
        backend:
          serviceName: es
          servicePort: 9200
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: frontend-websockets
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    kubernetes.io/ingress.allow-http: "false"
    # Set to one hour for websocket connections
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
spec:
  tls:
    - hosts:
      - {{ .Values.ingressHost }}
      secretName: ingress-tls
  rules:
  - host: {{ .Values.ingressHost }}
    http:
      paths:
      - path: /updates
        backend:
          serviceName: update-streamer
          servicePort: 8080
