---
apiVersion: v1
kind: Service
metadata:
  name: parceldata
  labels:
    app: parceldata
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  selector:
    app: parceldata
    release: "{{ .Release.Name }}"
  clusterIP: None
  ports:
  - name: http
    port: 8080
    targetPort: 8080
